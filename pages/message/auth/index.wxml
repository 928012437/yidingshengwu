<view class="page">
    <view class="inner">
        <view class="image">
            <image src="/static/images/accredit.png"></image>
        </view>
        <view class="text" wx:if="{{!close}}">您还未登录，请重新登录</view>
        <button bindtap="bind" class="btn" openType="getUserInfo" wx:if="{{!close}}">立即登录</button>

        <button bindtap="quxiao" class="btn" style="background: #9e9e9e;" >暂不登陆</button>

        <view class="text" wx:if="{{close}}">{{text}}</view>
    </view>
</view>
